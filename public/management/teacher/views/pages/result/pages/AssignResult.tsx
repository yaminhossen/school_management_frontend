import React from 'react';
import { Link } from 'react-router-dom';
export interface Props {}

const AssginResult: React.FC<Props> = (props: Props) => {
    return (
        <div className="admin_dashboard">
            <h3>AssginResult</h3>
        </div>
    );
};

export default AssginResult;
